class Todo {
  int id;
  String title;
  bool isDone = false;

  Todo({required this.id, required this.title, required this.isDone});
}

// List<Todo> todos = [
//   Todo(id: 1, title: "Beli Kucing", isDone: true)
// ]
